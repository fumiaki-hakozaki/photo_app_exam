<h1>写真＆ブログ詳細画面<h1>
<p>ユーザー名: <%= @post.user.name %></P>
<p>タイトル: <%= @post.title %></p>
<p>内容: <%= @post.content %></p>

<p id= "notice"><%= notice %></p>

  <strong>画像:</strong>
<p><%= image_tag @post.image.url %></p>


<% unless @post.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入りを解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りにする', favorites_path(post_id: @post.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>

<%= link_to '戻る', posts_path %>